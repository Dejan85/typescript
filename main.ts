export {};
const message = "hello world i xad";

let x: boolean = true;
let num: number = 2;
const name: string = "xad";

const sentence: string = `My name is ${name}. I am a biginner in Typscript`;

let n: null = null;
let u: undefined = undefined;

const list1: number[] = [1, 2, 3];
const list2: Array<number> = [1, 2, 3];

const person1: [string, number] = ["xad", 2];

enum Color {
  red,
  green,
  blue
}

let c: Color = Color.green;

let randomValue: any = 10;

randomValue = true;
randomValue = "xad";

let myVariable: any = 10;

// myVariable();
// myVariable.toUpperCase();

console.log(myVariable.name);
